@model IEnumerable<CartoonsWebApp.Models.User>
@{
    ViewBag.Title = "Список пользователей";
}

<a asp-action="Create">Добавить пользователя</a> |
<a asp-action="Index" asp-controller="Roles">Удалить роль</a>

<table class="table">
    <tr><th>Ім'я</th><th></th></tr>
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.Name</td>
            <td>
                <form asp-action="Delete" asp-route-id="@user.Id" method="post">
                    <a class="btn btn-sm btn-primary" style="" asp-action="Edit" asp-controller="Roles" asp-route-userId="@user.Id">Роль</a>
                    <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@user.Id">Изменить</a>
                    <a class="btn btn-sm btn-primary" asp-action="ChangePassword" asp-route-id="@user.Id">Сменить пароль</a>
                    <button type="submit" class="btn btn-sm btn-danger">
                        Удалить
                    </button>
                </form>
            </td>
        </tr>
    }
</table>

